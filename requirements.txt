truststore>=0.9      # nutzt den Windows-Zertifikatsspeicher (TLS-Intercept dieser Maschine)
requests>=2.31
PyYAML>=6.0
# playwright>=1.40    # nur nötig für die geplanten Browser-Quellen (tutti/ricardo/facebook)
